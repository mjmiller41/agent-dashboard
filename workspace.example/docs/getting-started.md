# Getting started

1. `npm install`
2. `npm run build && npm start`
3. Open `http://localhost:4680`

The dashboard reads and writes everything under `workspace/`. Coding agents
can edit `agents.json`, `sprints.json`, and friends directly — the UI picks
up changes live over SSE.
